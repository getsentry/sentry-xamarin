using Xamarin.Forms;

namespace Sample.Xamarin.Core.CustomControls
{
    /// <summary>
    /// Sadly this extra class is required since you can't scale things properly 
    /// with the default WebView.
    /// Custom render, here we go :D
    /// </summary>
    public class CustomWebView : WebView
    {
    }
}
